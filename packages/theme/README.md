# DECKPLATE Theme Tokens

This package serves as the Tailwind CSS configuration of the DECKPLATE design system. It contains a Tailwind `@theme` block that contains all the available design tokens.

It is intended to be the theme _source_ for all possible **DECKPLATE** packages.
