import { log } from "../utils/index.js";

// TODO: wrap to Shadcn call
export async function add(_component: string, _options: Record<string, unknown>): Promise<void> {
	return log.dim("NOT AVAILABLE");
	// const force = Boolean(options.force);
	// const cwd = process.cwd();
	// const projectRoot = findProjectRoot(cwd);
}
